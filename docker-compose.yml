services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command:
      - python
      - -m
      - reddit_reply_bot
      - --loop
      - --interval-seconds
      - "300"
      - --limit
      - "50"
